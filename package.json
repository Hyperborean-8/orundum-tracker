{
  "files": [
    "src/"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel serve src/index.html --public-url /orundum-tracker/",
    "build": "cross-env NODE_ENV=production parcel build src/index.html --dist-dir ./dist --public-url /orundum-tracker/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "gh-pages": "^1.2.0",
    "parcel": "^2.0.0",
    "typescript": "^2.9.2"
  }
}
